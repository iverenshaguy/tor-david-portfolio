const config = {
  extend: {
    boxShadow: {
      "custom-hero":
        "2px -1px 6px rgba(0, 0, 0, 0.1), -2px 1px 6px rgba(0, 0, 0, 0.1)",
    },
    fontFamily: {
      sans: ["var(--font-poppins)", "sans-serif"],
    },
  },
};

export default config;
